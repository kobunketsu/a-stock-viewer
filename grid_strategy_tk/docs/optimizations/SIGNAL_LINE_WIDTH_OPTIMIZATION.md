# 买卖分时信号线宽度优化

## 🎯 优化目标

将买卖分时信号线的宽度缩小一半，从2像素改为1像素，使信号线更加精细，不会过于突出影响图表美观。

## 🔧 具体修改

### 修改内容
- **买入信号线宽度**：从 `linewidth=2` 改为 `linewidth=1`
- **卖出信号线宽度**：从 `linewidth=2` 改为 `linewidth=1`

### 修改位置
1. **买入信号绘制**（第2201行）：
   ```python
   line = Line2D([x_pos, x_pos], [display_bottom, display_top],
                color=line_color, linewidth=1, linestyle=line_style, alpha=0.7, zorder=5)
   ```

2. **卖出信号绘制**（第2302行）：
   ```python
   line = Line2D([x_pos, x_pos], [display_bottom, display_top],
                color=line_color, linewidth=1, linestyle=line_style, alpha=0.7, zorder=5)
   ```

## 📊 优化效果

### 视觉效果改进
- **信号线更精细**：1像素宽度使信号线更加精细，不会过于突出
- **图表更美观**：减少信号线对价格图表的视觉干扰
- **保持可读性**：在保持信号清晰可见的同时，提升整体美观度

### 技术实现
- 使用 `replace_all=True` 确保所有相关代码都得到更新
- 保持其他属性不变（颜色、样式、透明度等）
- 语法检查通过，确保修改正确

## ✅ 验证结果

- ✅ 买入信号线宽度已修改为1像素
- ✅ 卖出信号线宽度已修改为1像素
- ✅ 语法检查通过
- ✅ 其他信号线属性保持不变

## 📝 总结

通过将买卖分时信号线的宽度从2像素缩小到1像素，我们成功优化了图表的视觉效果，使信号线更加精细美观，同时保持了良好的可读性和功能性。这个简单的调整提升了分时图的整体美观度，让用户能够更专注于价格走势的分析。